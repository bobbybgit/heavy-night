json.extract! event_setting, :id, :min_per_group, :max_per_group, :use_rec_player_count, :max_game_length, :min_game_length, :max_weight, :min_weight, :created_at, :updated_at
json.url event_setting_url(event_setting, format: :json)
